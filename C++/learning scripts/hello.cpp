#include <iostream> // preprocessor directive to include the iostream header file

using namespace std; // tells the compiler to use the standard (std) library of functions

int main() // defines a function called "main", and that that function returns an integer
{
	cout/* pronounced "C out" */<</* called "insertion operators" */"HEY! I'm alive.\n";
	cin.get(); /* function call to read user input. Expects user to press enter */

	return 1; /* returns a value of the function */
}

